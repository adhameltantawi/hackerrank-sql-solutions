# SQL Solutions

A personal repository for solving SQL problems across different difficulty levels, focused on writing clean and efficient queries.

## Topics Covered

- **Basic Queries** — `SELECT`, filtering with `WHERE`, sorting, and limiting results
- **Joins** — `INNER`, `LEFT`, `RIGHT`, and `FULL` joins across multiple tables

## Platforms

- **LeetCode**
- **HackerRank**
